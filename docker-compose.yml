version: "3.9"

services:

  mongodb:
    image: mongo:7
    container_name: mern-mongodb-2
    restart: always
    volumes:
      - mongo-data:/data/db
    networks:
      - mern-network
    ports:
      - "27019:27017"
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 5
  backend:
    image: ${BACKEND_IMAGE}
    container_name: mern-backend
    restart: always
    depends_on:
     - mongodb
    env_file:
     - ./backend/.env
    networks:
      - mern-network
    ports:
      - "5002:5000"
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
    user: "1000:1000"
    read_only: true

  frontend:
    image: ${FRONTEND_IMAGE}
    container_name: mern-frontend
    restart: always
    depends_on:
     - backend
    networks:
      - mern-network
    ports:
      - "3002:3000"
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
    user: "1000:1000"
    read_only: true

volumes:
  mongo-data:

networks:
  mern-network:
    driver: bridge
